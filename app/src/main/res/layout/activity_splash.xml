<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash_background">

    <!-- Glow circle behind logo -->
    <View
        android:id="@+id/glow_circle"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:background="@drawable/glow_circle"
        app:layout_constraintBottom_toBottomOf="@id/splash_logo"
        app:layout_constraintEnd_toEndOf="@id/splash_logo"
        app:layout_constraintStart_toStartOf="@id/splash_logo"
        app:layout_constraintTop_toTopOf="@id/splash_logo" />

    <!-- Logo -->
    <ImageView
        android:id="@+id/splash_logo"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_sirdaba"
        android:scaleType="fitCenter"
        android:contentDescription="SirDaba Delivery"
        app:layout_constraintBottom_toTopOf="@id/splash_tagline"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:layout_constraintVertical_bias="0.42" />

    <!-- Tagline -->
    <ImageView
        android:id="@+id/splash_tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/text_tagline"
        android:contentDescription="Distributor App"
        android:alpha="0"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/splash_logo" />

    <!-- Version -->
    <androidx.appcompat.widget.AppCompatTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="v1.0.0 — Distributor"
        android:textSize="11sp"
        android:textColor="#66333333"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
